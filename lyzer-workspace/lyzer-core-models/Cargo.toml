[package]
name = "lyzer-core-models"
version = "0.1.0"
edition = "2021"
description = "Canonical Constitutional Interfaces and Events"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
